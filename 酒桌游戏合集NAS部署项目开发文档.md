# 酒桌游戏合集 NAS 部署项目开发文档

## 1. 项目概述与部署要求

### 1.1 项目简介
- **项目名称**：酒桌游戏合集
- **项目类型**：极简 Web 应用，单页面多游戏集成，**移动端优先设计**
- **适用场景**：朋友聚会、家庭聚餐、商务宴请等酒桌场合的互动游戏，**主要面向手机等移动设备**
- **核心价值**：提供无需登录、开箱即用的酒桌游戏集合，支持多种常见游戏类型，**专为触屏操作优化**

### 1.2 部署环境要求
| 环境要求 | 具体说明 |
| :--- | :--- |
| **部署平台** | NAS 服务器（群晖、威联通、Unraid 等） |
| **运行方式** | 静态文件托管（推荐）或 Docker 容器 |
| **前置条件** | NAS 需开启静态文件服务功能（如群晖 Web Station、Nginx 服务等） |
| **网络要求** | 本地局域网访问，无需外网连接 |
| **客户端支持** | 支持现代浏览器（Chrome、Safari、Firefox、Edge 等） |

## 2. 功能模块设计

### 2.1 游戏切换方式
- **实现方式**：单页面内游戏切换，支持三种方案
  1. **顶部 Tab 切换**：游戏类型以标签页形式展示
  2. **卡片网格布局**：游戏以卡片形式排列，点击进入对应游戏
  3. **底部导航栏**：固定底部的游戏切换导航

### 2.2 摇骰子模块
- **核心功能**：
  - 支持自定义骰子数量（1-6个）
  - 显示点数总和或单个骰子结果
  - 动画效果：骰子滚动动画
- **交互流程**：
  1. 用户选择骰子数量
  2. 点击"摇骰子"按钮
  3. 显示骰子滚动动画
  4. 展示最终点数结果
  5. 自动记录到历史记录

### 2.3 猜数字模块
- **核心功能**：
  - 系统随机生成数字（默认 1-100，可调整范围）
  - 玩家输入猜测数字
  - 提示"大了/小了"
  - 记录猜测次数
- **交互流程**：
  1. 系统生成随机目标数字
  2. 玩家输入猜测数字并提交
  3. 系统判断并给出提示
  4. 重复步骤 2-3 直到猜中
  5. 显示猜中信息和总猜测次数
  6. 自动记录到历史记录

### 2.4 规则弹窗触发逻辑
- **触发机制**：
  - 首次访问游戏时自动弹出规则说明
  - 点击游戏区域右上角规则图标按钮手动唤起
- **LocalStorage 键名设计**：
  | 键名 | 说明 | 存储值 |
  | :--- | :--- | :--- |
  | `game_rules_seen_<game_id>` | 标记游戏规则是否已查看 | `true/false` |
  | `game_history_<game_id>` | 存储游戏历史记录 | JSON 格式 |

### 2.5 历史暂存逻辑
- **存储内容**：
  - 摇骰子：最后一次点数组合、总和
  - 猜数字：最后一轮猜测次数、是否猜中、目标数字
- **存储结构**：
  ```
  {
    "last_play_time": "2023-10-01T12:00:00",
    "result": {...}, // 游戏特定结果数据
    "metadata": {...} // 游戏元数据
  }
  ```
- **展示方式**：
  - 游戏操作面板下方显示"上次记录"卡片
  - 卡片包含"清除历史"按钮
  - 历史记录仅在当前浏览器有效

## 3. 技术架构说明

### 3.1 技术选型
| 模块 | 推荐方案 | 选型理由 |
| :--- | :--- | :--- |
| **前端框架** | 原生 HTML + JavaScript | 轻量优先，无需框架依赖，加载速度快，**适合移动端网络环境** |
| **UI 组件库** | 原生 CSS 或 Tailwind CSS | **保证移动端触摸友好**，样式定制灵活，响应式设计支持 |
| **状态管理** | 组件内状态 + LocalStorage 封装 | 无登录需求，无需全局状态管理库，**数据持久化适合移动端使用场景** |
| **构建工具** | 无（原生）或 Vite | 最终产出为静态文件，部署简单，**优化移动端加载性能** |
| **部署方式** | NAS 静态托管 | 无需后端服务，纯前端实现，**本地访问延迟低，适合移动端使用** |

### 3.2 目录结构规划
```
/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   ├── main.js         # 主逻辑
│   ├── games/          # 游戏模块
│   │   ├── dice.js     # 摇骰子游戏
│   │   ├── guess.js    # 猜数字游戏
│   │   └── index.js    # 游戏注册
│   └── utils/
│       ├── storage.js  # 本地存储封装
│       └── ui.js       # UI 工具函数
└── assets/
    └── images/         # 图片资源
```

### 3.3 本地存储封装设计
- **核心方法**：
  - `getItem(key)`：获取存储数据
  - `setItem(key, value)`：设置存储数据
  - `removeItem(key)`：删除存储数据
  - `clear()`：清空所有存储

## 4. 扩展性设计

### 4.1 新增游戏规范
- **游戏注册机制**：
  1. 在 `js/games/` 目录创建新游戏文件
  2. 实现游戏初始化、渲染、事件处理等方法
  3. 在 `js/games/index.js` 中注册游戏
- **游戏接口规范**：
  - `init()`：初始化游戏
  - `render()`：渲染游戏界面
  - `start()`：开始游戏
  - `stop()`：停止游戏
  - `getHistory()`：获取历史记录
  - `setHistory(data)`：设置历史记录

### 4.2 预留游戏类型
- 真心话大冒险
- 轮盘游戏
- 扑克抽牌
- 喝酒游戏

## 5. UI/UX 设计指南

### 5.1 页面整体布局（移动端优先）
- **顶部**：游戏标题、规则按钮，**固定定位，避免遮挡游戏区域**
- **中部**：游戏区域（根据选择的游戏动态变化），**垂直居中布局，适应竖屏显示**
- **底部**：游戏切换导航栏，**固定定位，便于单手操作**
- **弹窗**：规则说明弹窗、游戏结果弹窗，**全屏覆盖，适配小屏幕**

### 5.2 游戏卡片样式（移动端优化）
- **尺寸**：**触摸区域不小于 48×48px**，符合移动端触摸标准
- **样式**：简洁现代，适合聚会氛围，**高对比度，易于在不同光线环境下识别**
- **状态**：默认、选中、禁用三种状态，**状态变化明显，反馈及时**
- **动画**：轻微的点击反馈动画，**动画时长适中，避免过度动画影响体验**

### 5.3 移动端适配要点（详细说明）
- **响应式设计**：使用媒体查询适配不同屏幕尺寸，**优先针对手机竖屏优化**
- **触摸优化**：
  - 增大触摸区域，所有可点击元素最小尺寸不小于 48×48px
  - 支持手势操作，如滑动切换游戏、双击放大等
  - 避免密集的点击元素，确保操作区域有足够间距
- **横屏适配**：自动调整布局以适应横屏显示，**横屏时优化游戏区域布局**
- **性能优化**：
  - 减少重绘，保证动画流畅
  - 优化图片资源，减少加载时间
  - 使用 CSS 动画代替 JavaScript 动画，提升性能
- **字体与排版**：
  - 使用无衬线字体，提高屏幕可读性
  - 字体大小适中，确保在小屏幕上清晰可见
  - 关键信息突出显示，便于快速识别
- **网络优化**：
  - 减少外部资源依赖，确保离线可用
  - 优化静态资源大小，提升加载速度
  - 本地缓存策略，减少重复加载

## 6. NAS 部署步骤指引

### 6.1 群晖 Web Station 部署
1. 登录群晖 DSM 管理界面
2. 打开 **Web Station** 应用
3. 点击 **新增** 创建网站
4. 选择 **静态网站** 类型
5. 设置网站名称和文档根目录
6. 将项目文件上传到指定目录
7. 访问分配的本地 URL 即可使用

### 6.2 Docker + Nginx 部署
1. 创建 `docker-compose.yml` 文件
2. 配置 Nginx 容器，挂载项目目录
3. 运行 `docker-compose up -d` 启动容器
4. 通过 NAS IP 地址和端口访问应用

## 7. 测试用例建议

### 7.1 功能测试
- **规则弹窗测试**：
  1. 首次访问游戏时自动弹出规则
  2. 关闭后再次进入不弹出
  3. 点击规则按钮可手动弹出
- **历史记录测试**：
  1. 玩游戏后刷新页面，历史记录保持
  2. 清除历史后数据被删除
  3. 不同游戏历史记录相互独立
- **游戏功能测试**：
  1. 摇骰子：不同骰子数量测试
  2. 猜数字：边界值测试、输入校验
- **响应式测试**：
  1. 手机竖屏、横屏测试
  2. 平板、桌面端测试

### 7.2 性能测试
- **加载速度**：首次加载时间 < 2秒
- **动画流畅度**：骰子滚动动画无卡顿
- **交互响应**：按钮点击响应时间 < 100ms

### 7.3 兼容性测试
- 测试主流浏览器：Chrome、Safari、Firefox、Edge
- 测试不同版本的浏览器
- 测试不同网络环境下的表现